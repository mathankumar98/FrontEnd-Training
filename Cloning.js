//cloning is used to copy and create duplicate to the object is called cloning
// we use spread operator to achieve the cloning
let user={
    name:'Mathan',
    getfullname(user1)
    {
        user1.name='Mathan Kumar';
        console.log(user1);
    }
}
let another={...user}//spread operator
console.log(another);
